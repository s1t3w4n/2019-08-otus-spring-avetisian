spring:
  datasource:
    url: jdbc:h2:mem:testdb
    data: data_test.sql
    initialization-mode: always

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true

logging:
  level:
    ROOT: ERROR